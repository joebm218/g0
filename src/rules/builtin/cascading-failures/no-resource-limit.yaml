id: AA-CF-068
info:
  name: "No resource limits configured for agent execution"
  domain: cascading-failures
  severity: medium
  confidence: medium
  description: "No resource limits (CPU, memory) are configured for agent execution. Without resource constraints, a misbehaving agent can consume all available system resources, impacting co-located services and causing cascading failures."
  frameworks: [all]
  owasp_agentic: [ASI09]
  standards:
    nist_ai_rmf: [MEASURE-2.6]
check:
  type: agent_property
  property: maxIterations
  condition: missing
  message: "No resource limits (CPU, memory, iterations) configured for agent execution"
